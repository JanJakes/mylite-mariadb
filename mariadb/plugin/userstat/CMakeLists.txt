INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR}/sql)

OPTION(MYLITE_WITH_USERSTAT_DIAGNOSTICS "Build user statistics diagnostics" ON)
IF(MYLITE_WITH_USERSTAT_DIAGNOSTICS)
  MYSQL_ADD_PLUGIN(USERSTAT userstat.cc MANDATORY)
ENDIF()
