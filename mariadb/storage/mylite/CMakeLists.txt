SET(MYLITE_ROOT_DIR "${CMAKE_SOURCE_DIR}/..")
SET(MYLITE_STORAGE_DIR "${MYLITE_ROOT_DIR}/packages/mylite-storage")

INCLUDE_DIRECTORIES(
  "${MYLITE_STORAGE_DIR}/include"
  "${MYLITE_STORAGE_DIR}/src"
)

SET(MYLITE_SE_SOURCES
  ha_mylite.cc
  "${MYLITE_STORAGE_DIR}/src/storage.c"
)
MYSQL_ADD_PLUGIN(mylite_se ${MYLITE_SE_SOURCES} STORAGE_ENGINE STATIC_ONLY DISABLED)
