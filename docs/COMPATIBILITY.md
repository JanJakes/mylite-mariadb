# Compatibility

MyLite compatibility is tracked by surface area, not by broad claims. MariaDB
11.8 is the primary behavior authority; MySQL behavior is additional evidence
for drop-in application expectations.

## Status Key

- ✅&nbsp;Covered: implemented and covered by committed tests.
- 🟡&nbsp;Partial: implemented with documented limits and committed tests.
- ⚪&nbsp;Planned: target behavior for an upcoming slice.
- ➖&nbsp;Out&nbsp;of&nbsp;scope: deliberately omitted from the embedded
  single-file product.

## Validation

Compatibility evidence is grouped by the local harness documented in
[Compatibility Harness](architecture/compatibility-harness.md). The initial
groups cover public API validation, storage, crash recovery, locking, embedded
lifecycle, direct SQL including statement effects, prepared statements, column
metadata, large value reads, warnings, MariaDB baseline SQL API comparison,
storage-engine smoke including BLACKHOLE row discard and MEMORY/HEAP volatile
rows, sidecar gates, routed DDL/DML including schema namespaces,
directory-free file-backed `CREATE DATABASE`, and catalog-backed
`CREATE DATABASE` existence options,
`CREATE TABLE ... LIKE`, `CREATE TABLE ... SELECT` including representative
duplicate modes and FK-constrained targets for the current supported FK subset,
ordinary `CREATE TABLE IF NOT EXISTS`, representative plain
`CREATE OR REPLACE TABLE`, temporary LIKE/CTAS isolation, shadowing, and
representative temporary OR REPLACE, representative LIKE/CTAS OR REPLACE
replacement and failed replacement rollback, representative failed table-DDL
rollback, direct/prepared row-DML transaction commit/rollback and active-transaction
crash recovery, standalone routed
table-DDL `IF EXISTS` skip semantics, column ALTER
existence-option skips, index DDL including existence-option skips, index
rename DDL including existence-option skips, index ignorability metadata and
hint rejection, CHECK constraint ALTER existence-option skips, representative
non-CHECK primary/unique constraint DDL including primary-key ALTER add/drop
and failed ADD UNIQUE rollback,
transaction-control policy, prepared transaction lifecycle, literal and parameterized transaction `SET` controls, explicit temporary-table DDL in active transactions, and bounded direct/prepared savepoint rollback/release,
direct/prepared foreign-key DDL publication and handlerton advertising for the first validated `RESTRICT` / `NO ACTION`
subset including representative application-shaped ORM constraints with
explicit child keys, internal foreign-key handler metadata, `SHOW CREATE TABLE`
visibility, FK-aware copy ALTER metadata checks, FK-aware row checks including
basic and same-row self-references, ordered FK statement rollback including
representative multi-table ordering, bounded self-referencing
`ON DELETE SET NULL`, and parent truncate checks honoring
`foreign_key_checks=0`,
CHECK constraint enforcement, generated column
coverage, unsupported index-class rejection including long-unique hash keys and
vector indexes,
partition DDL rejection, online and in-place ALTER rejection, MariaDB statement
transaction hook integration, busy-timeout lock waits, SQL locking policy
rejection, failed
statement rollback, initial application-schema smoke including WordPress
single-site and multisite schema/seed fixtures, a BuddyPress component schema
fixture, Laravel default ORM schema fixtures, and Django default auth, admin,
and session schema fixtures, plus Rails Active Record component schema fixtures,
unsupported server, binlog/replication, replication/binlog filter assignment,
binlog/replication system-variable assignment and omitted-variable
introspection,
SQL file-I/O, server utility function, Oracle
SQL mode, XML SQL function, GIS SQL function, vector SQL function, SFORMAT SQL
function, JSON schema validation function, JSON table function, dynamic column function, SQL HANDLER
command, SQL sequence value surface, virtual sequence storage engine, dynamic
plugin loading, SQL HELP command, SELECT PROCEDURE,
table-maintenance/key-cache administration, native
CSV/InnoDB/MyISAM/MRG/partition engine absence, unsupported engine request
rejection, user-statistics,
statement profiling,
optimizer trace,
static SHOW information,
status metadata,
process-list metadata,
routine metadata,
foreign-server metadata,
external backup SQL,
query cache administration,
zlib compression,
stored-program runtime stubbing,
event parse-data validation stubbing,
dynamic UDF runtime, and non-table-object policies, and representative
`SHOW CREATE TABLE` round-trip export/import.
The opt-in embedded MTR smoke runner covers MariaDB bootstrap, scalar
CAST/CONVERT behavior, CASE-family expression behavior, selected numeric,
character/binary, interval, and type behavior, selected date,
temporal-rounding, temporal-function, and
time/datetime comparison behavior, selected parser/comment, keyword,
CTE-cycle, precedence, expression, type-coercion, rounding, safe-update, and
comparison behavior, selected `IN` / `NOT IN` predicate behavior, selected
subquery, update-ignore, `REPLACE`, `RETURNING`, temporary create-or-replace,
primary-key lookup, selected DDL/name behavior, ORDER BY, UNION, and
prepared-statement behavior, selected scalar operator behavior, selected
bit/extraction/replacement/regexp scalar-function behavior, selected
default-expression and weight-string behavior, selected string/format function
behavior, selected crypto/KDF function behavior, selected disabled DES
encryption-function behavior, selected aggregate DISTINCT behavior, selected
date-format behavior, selected ASCII, legacy, and UTF-32 charset edge behavior,
and selected charset diagnostics, collation-default behavior, and
column/context collation behavior, while MariaDB MTR comparison
suites and broader application-schema and runtime suites remain planned.

## Baseline

| Area | Target |
| --- | --- |
| MariaDB base | MariaDB 11.8 LTS, initial import ref `mariadb-11.8.6` / `9bfea48ce1214cc4470f6f6f8a4e30352cef84e7` |
| Runtime shape | Embedded in-process library, no daemon required for core use |
| Durable storage | One primary `.mylite` file, plus documented MyLite-owned rollback-journal, transaction-journal, lock, shared-memory, and temporary companions |
| Primary API | `libmylite` file-owned C API |
| MariaDB C API | Optional adapter, not the primary lifetime model |

## Public API

| Capability | MyLite status | Compatibility target |
| --- | --- | --- |
| Open and close a database file | 🟡&nbsp;Partial | Implemented for local files with a temporary MariaDB runtime directory that is removed on final close; repeated same-process open/close cycles and two-handle shared-runtime lifetime are covered |
| Direct SQL execution | 🟡&nbsp;Partial | `mylite_exec()` executes controlled one-shot SQL with textual result callbacks and direct statement-effect coverage in embedded builds |
| Prepared statements | 🟡&nbsp;Partial | Reusable embedded statements with 1-based scalar parameter binding, row stepping, reset/finalize ownership, MariaDB diagnostics including representative routed CHECK and generated-column execution failures, and representative baseline comparison coverage |
| Binary-safe values | 🟡&nbsp;Partial | Prepared statements expose explicit TEXT/BLOB byte counts, BLOB values with embedded NUL bytes, and current-row byte-range reads for large TEXT/BLOB results |
| Column metadata | 🟡&nbsp;Partial | Prepared statements expose alias, schema/table/origin names, MariaDB-native type, flags, charset, decimals, and length metadata; representative native type/name comparison is covered; prepared parameter counts are exposed, while rich parameter metadata is explicitly unsupported on the current MariaDB base because the MariaDB API returns no parameter metadata |
| Diagnostics | 🟡&nbsp;Partial | Open handles expose stable MyLite result codes, MariaDB errno, SQLSTATE, and message text |
| Warnings | 🟡&nbsp;Partial | Successful direct and prepared execution expose retained `SHOW WARNINGS` rows; failed direct execution, failed prepare, and failed prepared execute retain structured error rows before a result set is active; representative baseline comparison is covered, while fetch-time failure warning capture remains planned |
| Affected rows and insert ids | 🟡&nbsp;Partial | Successful direct and prepared execution expose affected rows for non-result statements and the last insert id; direct and prepared execution cover temporary-table insert/update/delete effects with representative MariaDB baseline comparison |
| Raw `MYSQL *` as primary API | ➖&nbsp;Out&nbsp;of&nbsp;scope | Available only through a deliberate compatibility adapter |

## Engine Routing

| SQL engine request | MyLite status | Target behavior |
| --- | --- | --- |
| MyLite engine registration | 🟡&nbsp;Partial | Opt-in static handler builds expose `MYLITE` through `SHOW ENGINES`; file-backed MyLite sessions configure it as the default effective engine |
| No explicit engine | 🟡&nbsp;Partial | `CREATE TABLE` routes to MyLite, stores requested engine `DEFAULT` with effective engine `MYLITE`, supports row insert, full scans, update/delete, truncate, copy `ALTER` rebuilds, `CREATE TABLE ... LIKE`, successful supported `CREATE TABLE ... SELECT`, BLOB/TEXT payloads, autoincrement, supported primary/unique/secondary indexes, and bounded BLOB/TEXT prefix indexes, and updates catalog metadata on `DROP TABLE` and simple `RENAME TABLE`; unsupported index classes remain explicit failures |
| `ENGINE=MYLITE` | 🟡&nbsp;Partial | Explicit MyLite DDL stores MariaDB table-definition metadata in the `.mylite` catalog, rediscovers it after reopen, supports row insert, full scans, update/delete, truncate, copy `ALTER` rebuilds, `CREATE TABLE ... LIKE`, successful supported `CREATE TABLE ... SELECT`, BLOB/TEXT payloads, autoincrement, supported primary/unique/secondary indexes, and bounded BLOB/TEXT prefix indexes, and updates catalog metadata on `DROP TABLE` and simple `RENAME TABLE`; unsupported index classes remain explicit failures |
| `ENGINE=InnoDB` | 🟡&nbsp;Partial | DDL routes to MyLite and records requested `InnoDB`; row insert, update/delete, truncate, copy `ALTER` rebuilds, `CREATE TABLE ... LIKE`, successful supported `CREATE TABLE ... SELECT`, BLOB/TEXT payloads, autoincrement, supported primary/unique/secondary indexes, bounded BLOB/TEXT prefix indexes, basic CHECK constraints, basic generated columns with ordinary generated-column secondary/unique indexes, full scans, failed row-DML statement rollback, direct/prepared row-DML `BEGIN` / `START TRANSACTION` / `COMMIT` / `ROLLBACK`, transaction restart through repeated direct or prepared `BEGIN` / `START TRANSACTION`, supported direct/prepared `START TRANSACTION READ WRITE` / `READ ONLY`, direct/prepared `COMMIT` / `ROLLBACK` `AND CHAIN`, `AND NO CHAIN`, and `NO RELEASE` modifiers, supported direct and prepared session `SET autocommit=0/1/DEFAULT` row-DML transactions, prepared single-marker `SET autocommit=?` transitions, mixed ordinary `SET` lists, and duplicate supported assignment lists applied in order with the final value as session state, direct/prepared `SET TRANSACTION` `READ WRITE` / `READ ONLY` and `ISOLATION LEVEL` forms, direct/prepared transaction read-only and isolation variable assignments including prepared single-marker values and duplicate supported assignment lists where the final value wins, direct/prepared session `SET completion_type=NO_CHAIN/0/DEFAULT/CHAIN/1` including prepared single-marker values and duplicate supported assignment lists where the final value wins, direct and prepared durable storage DDL rejection inside active transactions, explicit direct/prepared temporary table create/drop inside active transactions, read-only transaction rejection for direct and prepared durable MyLite storage writes with tracked temporary row-DML exceptions, direct and prepared savepoint rollback/release for case-insensitive simple unquoted, backtick-quoted, and ANSI_QUOTES double-quoted savepoint names, active row-DML transaction crash recovery, internal FK metadata, public direct/prepared `CREATE TABLE` and copy `ALTER TABLE ... ADD FOREIGN KEY` publication for validated `RESTRICT` / `NO ACTION` constraints over explicit or MariaDB-generated supported child keys and exact unique parent keys including basic and same-row self-references, MyLite handlerton FK support advertising for the documented subset, supported `ALTER TABLE ... DROP FOREIGN KEY` metadata removal, `SHOW CREATE TABLE` visibility, FK-aware copy ALTER metadata checks including handler-owned retained supporting-key validation, immediate child/parent row checks including prepared FK DML diagnostics with scalar-parameter checks, ordered multi-row child/self-reference FK checks with failed-statement rollback, representative ordered self-reference update/delete checks, representative non-self parent update/delete rollback ordering, representative multi-table parent-first/child-first FK update/delete ordering, bounded self-referencing `ON DELETE SET NULL`, session `foreign_key_checks=0` row-check and parent-truncate bypass without retrospective validation, generated supporting-key cleanup for explicit compatible-key replacement and explicit post-FK-drop index removal, `DROP TABLE`, and simple `RENAME TABLE` are covered; native InnoDB is not registered in the default embedded profile, and full InnoDB transaction semantics, handler-level savepoint hooks, global autocommit forms, direct-execution parameter markers, expression-valued or global parameterized transaction-control `SET` forms, bound `DEFAULT` / `RELEASE` transaction-control values, consistent snapshots, release completion, global transaction variable assignments and duplicate `SET TRANSACTION` characteristics, release completion-type defaults, durable transactional DDL, real storage isolation guarantees, broader exhaustive multi-table FK update/delete matrices, non-self `SET NULL`, `ON UPDATE SET NULL`, cascading actions, `SET DEFAULT`, MySQL-style expression indexes, FULLTEXT/SPATIAL access paths, full BLOB/TEXT indexes, and tablespaces remain unsupported; generated primary keys inherit MariaDB rejection, and unsupported foreign-key/index shapes including long-unique hash keys are rejected explicitly |
| `ENGINE=MyISAM` | 🟡&nbsp;Partial | DDL routes to MyLite and records requested `MyISAM`; row insert, update/delete, truncate, copy `ALTER` rebuilds, `CREATE TABLE ... LIKE`, successful supported `CREATE TABLE ... SELECT`, BLOB/TEXT payloads, full scans, supported primary/unique/secondary indexes, bounded BLOB/TEXT prefix indexes, `DROP TABLE`, and simple `RENAME TABLE` are covered, while native MyISAM and MRG_MyISAM engines are not registered in the default embedded profile and MyISAM data/index files plus native MyISAM maintenance administration are never durable application storage behavior |
| `ENGINE=Aria` | 🟡&nbsp;Partial | DDL routes to MyLite and records requested `Aria`; row insert, update/delete, truncate, copy `ALTER` rebuilds, `CREATE TABLE ... LIKE`, successful supported `CREATE TABLE ... SELECT`, BLOB/TEXT payloads, full scans, supported primary/unique/secondary indexes, bounded BLOB/TEXT prefix indexes, `DROP TABLE`, and simple `RENAME TABLE` are covered, while Aria data, index, and log files are never durable application storage |
| `ENGINE=BLACKHOLE` | 🟡&nbsp;Partial | DDL routes to MyLite and records requested `BLACKHOLE`; table metadata survives close/reopen, row writes are accepted but discarded, full scans and supported index reads return no rows, and no MariaDB BLACKHOLE data or index files are created; native BLACKHOLE replication/binlog behavior and broad native index capability parity remain out of scope |
| `ENGINE=MEMORY` / `ENGINE=HEAP` | 🟡&nbsp;Partial | DDL routes to MyLite and records the requested MEMORY/HEAP token; table metadata survives close/reopen, row contents and supported index entries are runtime-volatile and empty after embedded runtime shutdown/reopen, representative insert, full scan, forced B-tree-compatible index lookup, duplicate-key rejection, update/delete, truncate, and autoincrement reset are covered, and no durable MyLite row/index pages or MariaDB HEAP files are created; native MEMORY hash-index defaults, memory limits/accounting, BLOB/TEXT columns, replication/binlog behavior, and broader native parity remain unsupported or planned |
| `ENGINE=CSV` | ➖&nbsp;Out&nbsp;of&nbsp;scope | Native CSV table files are not registered in the default embedded profile because `.CSV` and `.CSM` sidecars do not fit the single-primary-file runtime; direct and prepared `CREATE TABLE ... ENGINE=CSV` and `ALTER TABLE ... ENGINE=CSV` requests, including MariaDB's no-equals `ENGINE CSV` spelling, are rejected before MariaDB execution and before MyLite catalog publication |
| `ENGINE=SEQUENCE` | ➖&nbsp;Out&nbsp;of&nbsp;scope | MariaDB's virtual generated-table `SEQUENCE` engine is not registered in the default embedded profile; explicit `ENGINE=SEQUENCE` and `ENGINE SEQUENCE` table requests fail before MariaDB execution and before MyLite catalog publication, while ordinary user tables with `seq_*` names can be catalog-backed MyLite tables |
| Dynamic external engines | ➖&nbsp;Out&nbsp;of&nbsp;scope | Unsupported explicit engine requests such as `ENGINE=ARCHIVE` and recognized no-equals forms for bundled optional engines such as `ENGINE ARCHIVE`, `ENGINE CONNECT`, `ENGINE FEDERATED`, `ENGINE MRG_MyISAM`, `ENGINE ROCKSDB`, and `ENGINE S3` fail before MariaDB execution and before MyLite catalog publication |

## File Ownership

| Capability | MyLite status | Target behavior |
| --- | --- | --- |
| Primary portable database file | 🟡&nbsp;Partial | Open/create writes and validates a versioned `.mylite` header, catalog root, table-definition records, child and parent-listable internal foreign-key metadata records, row pages, row-payload overflow pages, row-state pages, autoincrement state pages, and index-entry pages |
| Persistent `.frm` files | ➖&nbsp;Out&nbsp;of&nbsp;scope | Store table definitions in the MyLite catalog; metadata DDL smoke tests gate against durable `.frm` sidecars |
| Persistent InnoDB sidecars | ➖&nbsp;Out&nbsp;of&nbsp;scope | No `.ibd`, redo, undo, or independent tablespace files; the default embedded profile omits native InnoDB registration and runtime archive members, and metadata DDL smoke tests gate against known InnoDB sidecar names |
| Persistent MyISAM sidecars | ➖&nbsp;Out&nbsp;of&nbsp;scope | No `.MYD`, `.MYI`, or MRG_MyISAM durable table files; the default embedded profile omits native MyISAM and MRG_MyISAM registration, and metadata DDL smoke tests gate against those sidecars |
| Persistent Aria sidecars | ➖&nbsp;Out&nbsp;of&nbsp;scope | No `.MAI`, `.MAD`, `aria_log.*`, or Aria control state as application storage; metadata DDL smoke tests gate against those names |
| Persistent CSV sidecars | ➖&nbsp;Out&nbsp;of&nbsp;scope | No native `.CSV`, `.CSM`, or CSV update files; the default embedded profile omits native CSV registration and handler archive members |
| Persistent MEMORY/HEAP row files | ➖&nbsp;Out&nbsp;of&nbsp;scope | Routed MEMORY/HEAP tables store only durable table metadata in the primary `.mylite` file; row contents and supported index entries live in process memory and are cleared on embedded runtime shutdown |
| MyLite-owned companions | 🟡&nbsp;Partial | Bootstrap uses a MyLite-owned temporary MariaDB runtime directory, rollback journals and transaction journals are transient recovery companions, user temporary table rows/indexes live in process-local volatile MyLite state, supported file-backed schema creation/discovery, catalog-backed `CREATE DATABASE` existence options, and representative default-algorithm copy `ALTER` paths route from catalog records without runtime schema directories, and the runtime directory must be empty after final close in storage-engine smoke tests |

## SQL Surface

| Capability | MyLite status | Compatibility target |
| --- | --- | --- |
| `CREATE TABLE` metadata | 🟡&nbsp;Partial | Store MyLite table definitions for omitted/default engine, `ENGINE=MYLITE`, `ENGINE=InnoDB`, `ENGINE=MyISAM`, `ENGINE=Aria`, `ENGINE=BLACKHOLE`, and `ENGINE=MEMORY` / `ENGINE=HEAP` without durable MariaDB sidecars, cover ordinary `CREATE TABLE IF NOT EXISTS` missing-target creates and existing-target skips, cover successful representative plain `CREATE OR REPLACE TABLE` replacement, and cover representative catalog-backed `SHOW CREATE TABLE` export/import round trips |
| `DROP TABLE` | 🟡&nbsp;Partial | Remove MyLite catalog metadata for routed base tables without durable MariaDB sidecars, preserve the original table for a representative failed multi-table drop, and cover `DROP TABLE IF EXISTS` missing-table skips with warning rows; SQL-level FK coverage rejects referenced-parent drops without publishing partial catalog changes, drops child-table FK metadata with the child table, and allows the parent drop afterward; orphaned definition, row, index-entry, and FK metadata blob pages are not reclaimed yet |
| `RENAME TABLE` | 🟡&nbsp;Partial | Rename MyLite catalog metadata for simple routed base tables without durable MariaDB sidecars, preserving supported index reads and duplicate checks across close/reopen; FK metadata child and parent references are rewritten at the storage layer, a representative failed multi-table rename preserves the original name and rejects the intermediate new name, and `RENAME TABLE IF EXISTS` missing-source skips are covered with warning rows; trigger, partition, and broader rename semantics remain planned |
| `UPDATE` / `DELETE` / `REPLACE` | 🟡&nbsp;Partial | Full-scan and supported keyed update/delete are covered for routed base tables, including BLOB/TEXT payload rows, immediate child/parent row checks for retained FK metadata from storage seeding and supported public FK DDL, bounded self-referencing `ON DELETE SET NULL`, direct, prepared, and select-source failed-statement rollback for covered unique-key update and multi-row `REPLACE` failures, direct/prepared row-DML transaction commit/rollback through `BEGIN` and supported session autocommit mode, direct and prepared savepoint rollback/release for case-insensitive simple unquoted, backtick-quoted, and ANSI_QUOTES double-quoted savepoint names, active transaction crash recovery, and close/reopen visibility; broader transaction rollback, triggers, non-self FK actions, cascading actions, generated-column edge cases, and unsupported index classes remain planned |
| `TRUNCATE TABLE` | 🟡&nbsp;Partial | Logical truncate is covered for supported routed table shapes: live rows and index entries become invisible, autoincrement resets to the first generated value, catalog metadata is preserved, referenced parent tables are rejected through FK metadata checks while `foreign_key_checks=1`, `foreign_key_checks=0` allows parent truncation without retrospective orphan validation, and close/reopen visibility is tested; SQL rollback, physical compaction, and transaction-aware indexes remain planned |
| `ALTER TABLE` | 🟡&nbsp;Partial | Copy rebuilds are covered for supported MyLite-routed table shapes, including column add/drop/rename, representative `ADD COLUMN IF NOT EXISTS`, `DROP COLUMN IF EXISTS`, `MODIFY COLUMN IF EXISTS`, `CHANGE COLUMN IF EXISTS`, `RENAME COLUMN IF EXISTS`, and `ALTER COLUMN IF EXISTS SET/DROP DEFAULT` skips, generated-column add/modify/drop, supported base and generated-column index additions, drops, renames, secondary-index ignorability toggles, CHECK constraint add/drop existence-option skips, representative `ADD CONSTRAINT ... UNIQUE IF NOT EXISTS` and unique-key `DROP CONSTRAINT IF EXISTS` skips, failed `ADD CONSTRAINT ... UNIQUE` rollback over duplicate existing rows, representative primary-key add/drop/re-add DDL including duplicate `ADD PRIMARY KEY IF NOT EXISTS` warnings and failed re-add rollback over duplicate rows, supported `ADD CONSTRAINT ... FOREIGN KEY` publication for explicit or generated child-key `RESTRICT` / `NO ACTION` constraints, supported `DROP FOREIGN KEY` metadata removal, referenced parent unique secondary-key rename metadata updates for the supported FK subset, BLOB/TEXT payload rows, requested-engine metadata, close/reopen visibility, catalog-only reopened CHECK drop, failed ADD CHECK rollback over incompatible existing rows, representative catalog-only reopened default-algorithm column/index/autoincrement ALTER, FK-aware column/supporting-key rejection for retained FK metadata including handler-owned validation under MyLite's FK handlerton advertising, and rollback-journal publication; representative `LOCK=NONE`, `ALTER ONLINE TABLE`, and `ALGORITHM=INPLACE` / `INSTANT` / `NOCOPY` requests are rejected explicitly, while implementation of online/in-place algorithms, unsupported index classes, broader SQL rollback, and broader foreign-key actions remain planned |
| Partitioned tables | 🟡&nbsp;Partial | `CREATE TABLE ... PARTITION BY ...` and representative partition-management `ALTER TABLE` forms are rejected before MariaDB execution, and the default embedded profile omits native partition wrapper registration, until MyLite has partition metadata, partition routing, per-partition catalog lifecycle, and partition-aware row/index maintenance |
| Index add/drop DDL | 🟡&nbsp;Partial | Route supported copy-rebuild index additions and drops through MariaDB DDL and MyLite catalog/index updates, including generated-column indexes, MariaDB-generated FK child supporting indexes, named primary/unique constraint syntax in initial DDL, representative `ALTER TABLE ... ADD CONSTRAINT ... UNIQUE`, failed unique-constraint add rollback over duplicate rows, unique-key `DROP CONSTRAINT`, primary-key add/drop/re-add, standalone `CREATE INDEX IF NOT EXISTS`, ALTER-table `ADD INDEX IF NOT EXISTS`, and `DROP INDEX IF EXISTS` skips, representative default-algorithm standalone index DDL after catalog-only reopen, and rejection when dropping the last supporting key for retained FK metadata; online DDL, unsupported index classes, broader constraint matrices, SQL rollback, and broader foreign-key actions remain planned |
| `ALTER TABLE ... RENAME INDEX` | 🟡&nbsp;Partial | Route supported copy-rebuild base and generated-column secondary-index renames through MariaDB ALTER and MyLite catalog/index updates, including representative `RENAME INDEX IF EXISTS` skips and referenced parent unique secondary-key rename for the supported FK subset, preserving forced-index reads, duplicate-key checks, FK referenced-key metadata, row checks, and close/reopen discovery; primary-key rename, general rename conflict matrices, online DDL, unsupported index classes, SQL rollback, broader FK actions, and broader FK-specific conflict matrices remain planned |
| Ignored indexes | 🟡&nbsp;Partial | Preserve supported secondary-index `IGNORED` / `NOT IGNORED` metadata through `CREATE TABLE`, standalone `CREATE INDEX`, ALTER-table `ADD INDEX`, and `ALTER INDEX IF EXISTS` toggles, expose `INFORMATION_SCHEMA.STATISTICS.IGNORED`, reject index hints against ignored indexes, warn on missing `IF EXISTS` toggles, and rediscover state after close/reopen; primary-key, unsupported index classes, partitions, online/in-place algorithm variants, SQL rollback, and foreign keys remain planned |
| `CREATE TABLE ... LIKE` | 🟡&nbsp;Partial | Clone supported MyLite-routed table definitions without copying source rows, preserve source requested-engine metadata when no explicit engine is specified, reset target autoincrement state, cover cloned supported indexes before and after close/reopen, and cover FK child source-table clones that preserve table/index shape without copying FK metadata, successful representative `CREATE OR REPLACE TABLE ... LIKE`, and failed self-LIKE OR REPLACE target preservation; representative temporary-table LIKE catalog isolation, same-name shadowing, and temporary OR REPLACE shadow replacement are covered, while unsupported source objects, partitions, broader temporary-table variants, broader failed replacement variants, and SQL rollback remain planned |
| `CREATE TABLE ... SELECT` | 🟡&nbsp;Partial | Successful supported CTAS creates MyLite catalog metadata and inserts SELECT result rows through the normal handler write path, including no-engine and explicit `ENGINE=InnoDB` targets, BLOB/TEXT payloads, generated-source projections, explicit generated, CHECK-constrained, and FK-constrained target definitions for the current supported FK subset, autoincrement state, supported indexes, representative `IGNORE` / `REPLACE` duplicate-mode CTAS, close/reopen visibility, sidecar gates, and successful representative `CREATE OR REPLACE TABLE ... SELECT`; duplicate-key, CHECK-violation, and FK-violation CTAS abort removes target catalog metadata, statement checkpoints now restore covered failed statement visibility, representative failed replacement CTAS preserves old target visibility, and representative temporary CTAS catalog isolation, same-name shadowing, temporary OR REPLACE replacement, durable-source same-name temporary CTAS, and same-name repeated temporary CTAS OR REPLACE reopen-guard failure are covered, while unsupported source objects, unsupported foreign-key shapes/actions, partitions, broader duplicate-mode matrices, broader temporary-table variants, broader failed replacement variants, and broader SQL rollback remain planned |
| `LOAD DATA` / `LOAD XML` | ➖&nbsp;Out&nbsp;of&nbsp;scope | Server/client file import is not part of the core embedded file-owned API; direct and prepared LOAD statements are rejected before MariaDB execution with stable MyLite diagnostics, while ordinary INSERT, prepared DML, and supported CTAS paths remain the ingestion surfaces |
| `LOAD_FILE()` / `SELECT ... INTO OUTFILE` / `DUMPFILE` | ➖&nbsp;Out&nbsp;of&nbsp;scope | Host-file SQL I/O is outside the embedded file-owned API; direct and prepared statements using these surfaces are rejected before MariaDB execution with stable MyLite diagnostics, while ordinary result fetching and `SELECT ... INTO` user variables remain available |
| `CHECK` / `ANALYZE` / `OPTIMIZE` / `REPAIR TABLE`, `CACHE INDEX`, `LOAD INDEX INTO CACHE` | ➖&nbsp;Out&nbsp;of&nbsp;scope | Native table-maintenance and key-cache administration operates on server-owned engine files and key caches, not MyLite-routed table storage; direct and prepared statements, including representative `LOCAL` and `NO_WRITE_TO_BINLOG` table-maintenance forms, are rejected before MariaDB execution, and the default embedded profile omits native MyISAM maintenance, key-cache, preload, and engine registration objects while ordinary routed `ENGINE=MyISAM` DDL/DML remains available |
| SQL `HANDLER` commands | ➖&nbsp;Out&nbsp;of&nbsp;scope | Top-level `HANDLER ... OPEN`, `HANDLER ... READ`, and `HANDLER ... CLOSE` expose direct storage-engine cursors outside ordinary SQL planning and the file-owned API; direct and prepared statements are rejected before MariaDB execution, the default embedded profile links a fail-closed MyLite SQL-handler stub instead of the full `sql_handler.cc` implementation, and stored-program `DECLARE ... HANDLER` remains covered by the stored-program and non-table-object policy |
| SQL `HELP` | ➖&nbsp;Out&nbsp;of&nbsp;scope | Server help-table lookup is not part of the core embedded profile; the default embedded profile links a MyLite stub instead of the full `sql_help.cc` help-table scanner, and direct/prepared `HELP` commands are rejected before MariaDB execution while quoted mentions remain ordinary string literals |
| `SELECT ... PROCEDURE` / `PROCEDURE ANALYSE()` | ➖&nbsp;Out&nbsp;of&nbsp;scope | Server-side SELECT result-set procedure hooks are not part of the core embedded profile; the default embedded profile links a MyLite `setup_procedure()` stub instead of the full `procedure.cc` dispatcher and omits the full `sql_analyse.cc` analyser, and direct/prepared `SELECT ... PROCEDURE` statements are rejected before MariaDB execution while ordinary SELECT statements remain available |
| Server utility functions | ➖&nbsp;Out&nbsp;of&nbsp;scope | Blocking, timing, named-lock, replication-wait, and server-identity utility functions such as `BENCHMARK()`, `SLEEP()`, `UUID_SHORT()`, `MASTER_POS_WAIT()`, and `MASTER_GTID_WAIT()` are rejected before MariaDB execution with stable MyLite diagnostics; named-lock helpers such as `GET_LOCK()` and `RELEASE_LOCK()` remain rejected by the SQL locking policy, while ordinary scalar helpers such as `VERSION()` remain available |
| Oracle SQL mode | ➖&nbsp;Out&nbsp;of&nbsp;scope | Oracle Database compatibility mode is not part of MyLite's MySQL/MariaDB embedded profile; the default embedded profile links a MyLite `ORAparse()` stub instead of the generated Oracle parser, and direct/prepared `SET sql_mode` assignments that include the `ORACLE` mode token are rejected before MariaDB execution while ordinary MySQL/MariaDB SQL modes remain available |
| XML SQL functions | ➖&nbsp;Out&nbsp;of&nbsp;scope | Legacy XML XPath helpers `EXTRACTVALUE()` and `UPDATEXML()` are not part of the core embedded profile; the default embedded profile omits `item_xmlfunc.cc`, and direct/prepared calls are rejected before MariaDB execution while quoted mentions and separate file-import policy for `LOAD XML` remain unchanged |
| GIS SQL functions | ➖&nbsp;Out&nbsp;of&nbsp;scope | GIS constructor, conversion, predicate, measurement, and set-operation helpers such as `ST_AsText()`, `ST_GeomFromText()`, `ST_Contains()`, `PointFromText()`, `Point()`, and `X()` are not part of the current core embedded profile; the default embedded profile omits `item_geofunc.cc` and `gcalc_*` helpers, and direct/prepared calls are rejected before MariaDB execution while geometry type parsing and SPATIAL-index rejection remain separate DDL policy |
| Vector SQL functions and MHNSW indexes | ➖&nbsp;Out&nbsp;of&nbsp;scope | MariaDB vector conversion, distance, and HNSW index runtime is not part of the current core embedded profile; the default embedded profile omits `item_vectorfunc.cc`, `vector_mhnsw.cc`, and mandatory `mhnsw` plugin registration, direct/prepared `VEC_FROMTEXT()`, `VEC_TOTEXT()`, `VEC_DISTANCE()`, `VEC_DISTANCE_EUCLIDEAN()`, and `VEC_DISTANCE_COSINE()` calls are rejected before MariaDB execution, and routed vector-index DDL rejects before MyLite catalog publication while `VECTOR(N)` type parsing remains a separate compatibility decision |
| `SFORMAT()` | ➖&nbsp;Out&nbsp;of&nbsp;scope | MariaDB's fmtlib-backed `SFORMAT()` helper is not part of the current core embedded profile; the default embedded profile omits its builder, registry entry, and implementation so retained `sql_embedded` C++ sources can compile without exceptions, and direct/prepared calls are rejected before MariaDB execution while ordinary `FORMAT()`, `DATE_FORMAT()`, `TIME_FORMAT()`, and `GET_FORMAT()` remain available |
| `JSON_SCHEMA_VALID()` | ➖&nbsp;Out&nbsp;of&nbsp;scope | JSON schema validation is not part of the current core embedded profile; the default embedded profile omits `json_schema.cc`, and direct/prepared calls are rejected before MariaDB execution while ordinary JSON helpers such as `JSON_VALID()` remain available |
| `JSON_TABLE()` | ➖&nbsp;Out&nbsp;of&nbsp;scope | JSON table projection is not part of the current core embedded profile; the default embedded profile replaces `json_table.cc` with a fail-closed MyLite stub, and direct/prepared calls are rejected before MariaDB execution while ordinary JSON scalar/path helpers remain available |
| Dynamic column functions | ➖&nbsp;Out&nbsp;of&nbsp;scope | MariaDB dynamic-column packed BLOB helpers such as `COLUMN_CREATE()`, `COLUMN_GET()`, `COLUMN_ADD()`, `COLUMN_DELETE()`, `COLUMN_CHECK()`, `COLUMN_EXISTS()`, `COLUMN_JSON()`, and `COLUMN_LIST()` are not part of the current core embedded profile; the default embedded profile replaces `ma_dyncol.c` with fail-closed MyLite API stubs, and direct/prepared calls are rejected before MariaDB execution while ordinary BLOB values remain available |
| Sequences | ➖&nbsp;Out&nbsp;of&nbsp;scope | Sequence objects and sequence value expressions/functions are not part of the current core embedded profile until MyLite has catalog-backed sequence metadata and durable value persistence; direct/prepared `CREATE SEQUENCE`, `ALTER SEQUENCE`, `DROP SEQUENCE`, `NEXT VALUE FOR`, `PREVIOUS VALUE FOR`, `NEXTVAL()`, `LASTVAL()`, and `SETVAL()` surfaces are rejected before MariaDB execution, and the default embedded profile replaces `sql_sequence.cc` / `ha_sequence.cc` with a fail-closed MyLite sequence stub while ordinary `AUTO_INCREMENT` remains available |
| Virtual sequence tables | ➖&nbsp;Out&nbsp;of&nbsp;scope | MariaDB's `SEQUENCE` storage engine generates virtual tables such as `seq_1_to_10` through storage-engine discovery instead of MyLite catalog metadata; the default embedded profile disables `PLUGIN_SEQUENCE`, `SHOW ENGINES` does not advertise `SEQUENCE`, and generated virtual sequence tables remain unavailable until MyLite has a catalog-aware series design |
| Schemas/databases | 🟡&nbsp;Partial | Direct and prepared `CREATE DATABASE` / `CREATE SCHEMA` store catalog namespace records with default character set, collation, and comment options, file-backed schema creation, reopen, `USE`, `SHOW DATABASES`, `SHOW TABLES`, `SHOW CREATE DATABASE`, information-schema schema options, table resolution, catalog-backed `CREATE DATABASE` duplicate, `IF NOT EXISTS`, and `OR REPLACE` paths, and representative default-algorithm copy `ALTER` paths work without runtime schema directories, and direct and prepared `DROP DATABASE` / `DROP SCHEMA` remove covered catalog metadata; non-table objects and SQL rollback remain planned |
| Views, triggers, and routines | 🟡&nbsp;Partial | Catalog-backed persistent objects remain planned after table DDL is stable; current `libmylite` entry points reject representative view, trigger, routine, package, `CALL`, and view/routine/trigger `SHOW CREATE` statements before MariaDB can publish filesystem or server-table metadata, `INFORMATION_SCHEMA.VIEWS`, routine status, routine information-schema metadata, `SHOW TRIGGERS`, and `INFORMATION_SCHEMA.TRIGGERS` remain visible with zero rows, and the default embedded profile links fail-closed MyLite view-runtime, stored-program, and trigger-runtime stubs instead of MariaDB's file-backed view runtime, full stored-program compiler/runtime, and file-backed trigger runtime |
| Dynamic UDFs | ➖&nbsp;Out&nbsp;of&nbsp;scope | Dynamic user-defined function registration, `mysql.func` lookup, external library loading, and UDF execution are not part of the embedded profile; direct and prepared `CREATE FUNCTION ... SONAME` calls are rejected before MariaDB execution, the default embedded profile omits `sql_udf.cc` and UDF item construction/execution bodies, and ordinary native-function plus stored-function resolution remains unchanged |
| Events and scheduler | ➖&nbsp;Out&nbsp;of&nbsp;scope | Server scheduler is not part of the core embedded profile; event activation and event DDL are rejected by MyLite SQL policy; the default embedded profile replaces MariaDB's event parse-data validation with a fail-closed MyLite stub so raw embedded parser paths do not become partial event catalog support |
| Users, grants, and password auth | ➖&nbsp;Out&nbsp;of&nbsp;scope | Local embedded file ownership replaces server account management; account SQL is rejected by MyLite SQL policy |
| Replication and binlog | ➖&nbsp;Out&nbsp;of&nbsp;scope | Server topology feature, not core library behavior; binlog is disabled at startup, `log_bin` reports `OFF`, representative replication/binlog SQL plus replication/binlog filter and broader binlog/replication system-variable assignments are rejected, representative disabled variables such as `binlog_format`, `gtid_binlog_state`, `relay_log`, and `replicate_do_db` are omitted from `SHOW VARIABLES`, and the default embedded profile omits `gtid_index.cc`, `log_event.cc`, `log_event_server.cc`, `rpl_gtid.cc`, `rpl_injector.cc`, `rpl_record.cc`, mandatory binlog plugin registration, and binlog transaction, row-event, GTID-state, event-write, table-map, open/recovery, GTID-index, incident, cache-write, temporary-table, retained SQL `BINLOG` event parsing, retained binlog GTID state, replication filter rule parsing/storage bodies, and disabled binlog/replication system-variable registration behind no-op, permissive, or fail-closed MyLite stubs and guards |
| Dynamic plugin installation/loading | ➖&nbsp;Out&nbsp;of&nbsp;scope | Dynamic plugin loading is compiled out of the default embedded profile: generated embedded configuration clears `HAVE_DLOPEN`, `HAVE_DLADDR`, `HAVE_DLERROR`, and `HAVE_DLFCN_H`, `have_dynamic_loading` reports `NO`, static built-ins remain available, core startup uses a MyLite-owned empty plugin directory, and representative direct/prepared plugin install/uninstall SQL is rejected before MariaDB execution |
| Performance schema | ➖&nbsp;Out&nbsp;of&nbsp;scope | Server instrumentation tables are disabled or compiled out in the default embedded runtime profile |
| User statistics | ➖&nbsp;Out&nbsp;of&nbsp;scope | MariaDB's user-statistics information-schema plugin is a server-observability surface, not core embedded file-owned behavior; the default embedded profile omits `userstat.cc`, direct/prepared `SHOW *_STATISTICS`, `FLUSH *_STATISTICS`, `userstat` system-variable assignment, and `INFORMATION_SCHEMA` user-statistics table access are rejected before MariaDB execution, and ordinary SQL user variables such as `@userstat` remain available |
| Statement profiling | ➖&nbsp;Out&nbsp;of&nbsp;scope | MariaDB's statement profiling is a session observability surface, not core embedded file-owned behavior; the default embedded profile builds with `ENABLED_PROFILING=OFF`, reports `have_profiling=NO`, and direct/prepared `SHOW PROFILE`, `SHOW PROFILES`, profiling system-variable assignment, and `INFORMATION_SCHEMA.PROFILING` access are rejected before MariaDB execution |
| Optimizer trace | ➖&nbsp;Out&nbsp;of&nbsp;scope | MariaDB's optimizer trace is a session diagnostics surface, not core embedded file-owned behavior; the default embedded profile replaces `opt_trace.cc` with `mylite_opt_trace_disabled.cc`, and direct/prepared optimizer-trace system-variable assignment and `INFORMATION_SCHEMA.OPTIMIZER_TRACE` access are rejected before MariaDB execution |
| Static SHOW information | ➖&nbsp;Out&nbsp;of&nbsp;scope | MariaDB's `SHOW AUTHORS`, `SHOW CONTRIBUTORS`, and `SHOW PRIVILEGES` commands expose server attribution, sponsor, and privilege metadata, not core embedded file-owned behavior; the default embedded profile compiles out their static result producers, and direct/prepared access is rejected before MariaDB execution |
| Status metadata | ➖&nbsp;Out&nbsp;of&nbsp;scope | MariaDB's `SHOW STATUS`, `SHOW GLOBAL STATUS`, `SHOW SESSION STATUS`, `INFORMATION_SCHEMA.GLOBAL_STATUS`, and `INFORMATION_SCHEMA.SESSION_STATUS` expose server/session observability counters, not core embedded file-owned behavior; the default embedded profile compiles out the status publication table and dynamic status-variable registry, and the status SHOW and information-schema surfaces remain visible with zero rows |
| Process-list metadata | ➖&nbsp;Out&nbsp;of&nbsp;scope | MariaDB's `SHOW PROCESSLIST`, `SHOW FULL PROCESSLIST`, and populated `INFORMATION_SCHEMA.PROCESSLIST` rows expose daemon thread/session inventory, not core embedded file-owned behavior; the default embedded profile compiles out the process-list row producers, direct/prepared process-list SHOW commands are rejected before MariaDB execution, and `INFORMATION_SCHEMA.PROCESSLIST` remains visible with zero rows |
| Routine metadata | ➖&nbsp;Out&nbsp;of&nbsp;scope | MariaDB's `SHOW PROCEDURE STATUS`, `SHOW FUNCTION STATUS`, `INFORMATION_SCHEMA.ROUTINES`, and `INFORMATION_SCHEMA.PARAMETERS` expose stored routine catalog metadata backed by `mysql.proc`; the default embedded profile compiles out the `mysql.proc` scan helpers and returns zero rows for those status and information-schema surfaces until MyLite has catalog-backed routine metadata |
| Foreign-server metadata | ➖&nbsp;Out&nbsp;of&nbsp;scope | MariaDB's `mysql.servers` cache is global remote-engine metadata, not MyLite file-owned application table state; the default embedded profile replaces `sql_servers.cc` with `mylite_sql_servers_disabled.cc`, embedded startup does not read `mysql.servers`, and direct/prepared `CREATE SERVER`, `CREATE OR REPLACE SERVER`, `ALTER SERVER`, `DROP SERVER`, and `SHOW CREATE SERVER` are rejected before MariaDB execution |
| External backup SQL | ➖&nbsp;Out&nbsp;of&nbsp;scope | MariaDB's `BACKUP STAGE`, `BACKUP LOCK`, and `BACKUP UNLOCK` statements coordinate external physical backup tools against a datadir and engine-owned table files, not MyLite's file-owned embedded API; the default embedded profile replaces `backup.cc` with `mylite_backup_disabled.cc`, direct/prepared backup commands are rejected before MariaDB execution, and retained DDL backup hooks are no-ops so ordinary DDL remains available |
| Query cache | ➖&nbsp;Out&nbsp;of&nbsp;scope | MariaDB's query cache is a server-global `SELECT` result cache, not durable file-owned MyLite behavior; the default embedded profile replaces `sql_cache.cc` with `mylite_query_cache_disabled.cc`, omits `emb_qcache.cc`, reports `have_query_cache=NO`, rejects direct/prepared `FLUSH QUERY CACHE`, `RESET QUERY CACHE`, and query-cache system-variable assignment before MariaDB execution, and leaves ordinary `SELECT`, `SELECT SQL_CACHE`, and `SELECT SQL_NO_CACHE` available as uncached execution paths |
| Zlib compression | ➖&nbsp;Out&nbsp;of&nbsp;scope | Zlib-backed SQL, protocol, binlog, and compressed-column storage are not part of the current core embedded profile; the default embedded profile reports `have_compress=NO`, makes `COMPRESS()` and `UNCOMPRESS()` return `NULL`, rejects direct/prepared compressed column DDL with an unsupported diagnostic, compiles retained compression paths as fail-closed no-zlib code, and linked first-party embedded smoke binaries do not link libz |

## Rows, Indexes, And Constraints

| Capability | MyLite status | Compatibility target |
| --- | --- | --- |
| Keyless table scans | 🟡&nbsp;Partial | Insert, full-scan, update, and delete rows for MyLite-routed tables without declared keys, with values persisted in the primary `.mylite` file across close/reopen |
| Volatile MEMORY/HEAP rows | 🟡&nbsp;Partial | Runtime-only row and supported index storage is covered for representative MEMORY/HEAP tables, including empty-table reopen after embedded runtime shutdown, duplicate-key checks, forced-index reads, update/delete, truncate, and autoincrement reset; native MEMORY hash-index defaults, memory limits/accounting, BLOB/TEXT columns, and transactional/savepoint semantics remain unsupported or planned |
| Fixed and variable row fields | 🟡&nbsp;Partial | Store MariaDB record-compatible row images for routed rows, including nullable fixed fields, variable fields, supported keyed rows, and BLOB/TEXT payloads covered by smoke tests |
| NULL columns | 🟡&nbsp;Partial | Preserve SQL NULL values for routed row inserts, updates, deletes, and full scans before and after reopen, including BLOB/TEXT fields and nullable unique-key semantics |
| BLOB/TEXT values | 🟡&nbsp;Partial | Routed rows persist `TEXT`/`BLOB` values in primary-file row payloads, including NULL, empty, binary, large overflow values, generated BLOB/TEXT values, update/delete, and bounded BLOB/TEXT prefix indexes; unbounded unique BLOB/TEXT long-unique hash indexes reject before catalog publication, while full BLOB/TEXT index support and broader binary-safe public value APIs remain planned |
| Primary and secondary indexes | 🟡&nbsp;Partial | Supported primary and secondary keys use durable append-only MyLite index-entry pages plus ordered in-memory handler cursors, including named primary/unique constraint syntax in initial DDL, `ALTER TABLE ... ADD CONSTRAINT ... UNIQUE`, failed unique-constraint add rollback over duplicate rows, unique-key `DROP CONSTRAINT`, primary-key add/drop/re-add through copy ALTER, standalone copy-rebuild index DDL, bounded BLOB/TEXT prefix indexes, bounded generated BLOB/TEXT prefix indexes through initial and standalone copy-rebuild DDL, ordinary generated-column secondary/unique indexes, generated FK supporting-key cleanup, ignored secondary-index metadata and hint rejection, and logical truncate filtering; generated primary-key DDL inherits MariaDB rejection before catalog publication; unbounded unique BLOB/TEXT long-unique hash keys, FULLTEXT, SPATIAL, and vector indexes reject before catalog publication; B-tree navigation, full BLOB/TEXT index support, MySQL-style expression-index compatibility, physical FULLTEXT/SPATIAL/vector access paths, and compaction remain planned |
| Unique indexes | 🟡&nbsp;Partial | Duplicate-key checks are covered for supported unique indexes, including nullable unique-key semantics where NULL values do not conflict unless MariaDB marks NULLs equal |
| Autoincrement | 🟡&nbsp;Partial | Durable table-local state is covered for single-column and first-key compound autoincrement keys, including BLOB/TEXT payload rows, explicit high values, `ALTER TABLE ... AUTO_INCREMENT` before and after catalog-only reopen, preservation across copy `ALTER`, close/reopen, supported keyed update/delete, truncate reset, and explicit rejection of grouped later-in-key autoincrement definitions before catalog publication; grouped per-prefix allocation semantics and transaction-aware rollback remain planned |
| CHECK constraints | 🟡&nbsp;Partial | Basic column-level and named table-level CHECK constraints on routed tables are enforced by MariaDB before MyLite handler writes, survive close/reopen through catalog-backed table-definition metadata, honor `check_constraint_checks=OFF`, support named table-level CHECK add/drop through copy `ALTER` including `ADD CONSTRAINT IF NOT EXISTS` and `DROP CONSTRAINT IF EXISTS` skips plus reopened drops of ALTER-added checks, cover failed ADD CHECK rollback over incompatible existing rows, prepared execution diagnostics, representative deterministic expression matrices, representative dump-style import, representative `SHOW CREATE TABLE` round-trip export/import, and explicit CHECK-constrained CTAS targets, and roll back earlier rows in covered failed multi-row statements; exhaustive expression, broader dump/export, and multi-statement rollback coverage remain planned |
| Foreign keys | 🟡&nbsp;Partial | Public direct/prepared `CREATE TABLE`, explicit FK-constrained `CREATE TABLE ... SELECT` targets, copy `ALTER TABLE ... ADD CONSTRAINT ... FOREIGN KEY`, and `ALTER TABLE ... DROP FOREIGN KEY` work for validated `RESTRICT` / `NO ACTION` constraints over explicit or MariaDB-generated MyLite-supported child key prefixes and exact unique parent keys, plus bounded self-referencing `ON DELETE SET NULL` over simple nullable child columns. Covered DDL includes representative Laravel-, Django-, and Rails-shaped FK constraints, column-level `REFERENCES`, basic self-references where the parent row already exists, and same-row self-references where the child and parent key prefixes match; FK metadata records persist, read, list, parent-list, drop, rename child/parent FK identities, and rewrite referenced parent key names inside the primary `.mylite` file; MyLite advertises `HTON_SUPPORTS_FOREIGN_KEYS` for the documented subset, and handler hooks map FK records to MariaDB `FOREIGN_KEY_INFO` for information-schema rows, parent-table metadata checks, `SHOW CREATE TABLE` FK clauses, FK-aware copy ALTER column/supporting-key rejection with handler-owned retained supporting-key validation including referenced parent unique secondary-key rename, metadata preservation across covered copy rebuilds and successful FK-constrained CTAS, failed FK-constrained CTAS target cleanup, generated supporting-key cleanup when explicit compatible keys replace them or after explicit post-FK-drop index removal, immediate row checks for covered child insert/update and parent update/delete paths including prepared FK DML diagnostics with scalar-parameter checks, ordered multi-row child/self-reference FK checks with failed-statement rollback, representative ordered self-reference update/delete checks, representative non-self parent update/delete rollback ordering, representative multi-table parent-first/child-first FK update/delete ordering, self-referencing truncate, and session `foreign_key_checks=0` row-check and parent-truncate bypass without retrospective validation; `libmylite` still rejects `CREATE TEMPORARY TABLE` FK DDL, while broader exhaustive multi-table FK update/delete matrices, non-self `SET NULL`, `ON UPDATE SET NULL`, cascading actions, `SET DEFAULT`, and broader transaction-aware checks remain planned |
| Generated columns | 🟡&nbsp;Partial | Basic virtual and stored generated columns on routed tables are handled through MariaDB generated-column evaluation, MyLite row storage, and catalog-backed table-definition metadata across insert, update, full-scan read, generated-source CTAS projection, generated target CTAS definitions, copy-rebuild add/modify/drop, representative deterministic expression matrices, representative dump-style import, representative `SHOW CREATE TABLE` round-trip export/import, and close/reopen; ordinary scalar generated-column secondary/unique indexes and bounded generated BLOB/TEXT prefix indexes are covered for initial DDL, ALTER-backed additions, standalone create/drop, rename, forced-index reads, duplicate checks, prepared execution diagnostics, update/delete maintenance, and reopen; generated primary-key create/alter DDL inherits MariaDB rejection without catalog publication, and generated unbounded BLOB/TEXT long-unique hash indexes reject before catalog publication; exhaustive expressions, broader dump/export, and rollback remain planned |
| FULLTEXT, SPATIAL, vector, and long-unique indexes | 🟡&nbsp;Partial | Initial routed `CREATE TABLE` definitions with FULLTEXT, SPATIAL, vector, or unbounded unique BLOB/TEXT long-unique hash indexes reject before MyLite catalog publication, and long-unique standalone/copy-rebuild additions leave existing tables unchanged; physical or well-defined metadata-backed access paths remain planned |

## Transactions, Recovery, And Concurrency

| Capability | MyLite status | Compatibility target |
| --- | --- | --- |
| Atomic commit | 🟡&nbsp;Partial | Rollback journal protects current catalog, row, row-state, truncate, autoincrement, and index-entry publication; transaction journals protect active file-backed row-DML transactions until commit removes the journal; statement checkpoints protect covered failed file-backed SQL statements; autocommit row-DML checkpoints commit through MariaDB statement transaction hooks; direct/prepared `BEGIN` / `START TRANSACTION` / `COMMIT`, transaction restart through repeated direct or prepared `BEGIN` / `START TRANSACTION`, supported direct/prepared `START TRANSACTION READ WRITE` / `READ ONLY`, direct/prepared `SET TRANSACTION` `READ WRITE` / `READ ONLY` and `ISOLATION LEVEL` forms, direct/prepared transaction read-only and isolation variable assignments including prepared single-marker values and duplicate supported assignment lists where the final value wins, direct/prepared session `SET completion_type=NO_CHAIN/0/DEFAULT/CHAIN/1` including prepared single-marker values and duplicate supported assignment lists where the final value wins, direct/prepared `COMMIT AND CHAIN`, `COMMIT AND NO CHAIN`, `COMMIT NO RELEASE`, supported direct/prepared `SET autocommit=0` / direct or prepared `COMMIT`, `SET autocommit=1`, or `SET autocommit=DEFAULT` row-DML transactions, prepared single-marker `SET autocommit=?` transitions, and duplicate supported assignment lists applied in order with the final value as session state, and direct or prepared `RELEASE SAVEPOINT` release nested MyLite storage checkpoints |
| Rollback | 🟡&nbsp;Partial | Covered failed file-backed statements restore the pre-statement MyLite header/catalog snapshot, including rows, row-state pages, index entries, autoincrement pages, and catalog records appended after the checkpoint; covered direct, prepared, and select-source row-DML rollback is driven by statement checkpoints, including representative failed multi-row `REPLACE` statements after an earlier replacement in the same statement; direct/prepared `BEGIN` / `START TRANSACTION` / `ROLLBACK`, supported direct/prepared `ROLLBACK AND CHAIN`, `ROLLBACK AND NO CHAIN`, `ROLLBACK NO RELEASE`, and supported direct/prepared `SET autocommit=0` / direct or prepared `ROLLBACK` row-DML transactions and prepared single-marker `SET autocommit=?` rollback transitions restore an outer MyLite checkpoint, direct/prepared explicit temporary table create/drop inside active transactions remains session-local and outside durable rollback, direct and prepared `ROLLBACK TO [SAVEPOINT]` restore nested row-DML savepoint checkpoints while keeping the target savepoint active, and close-time rollback of active transactions and savepoints is covered; pending transaction journals roll back uncommitted active row-DML transactions after process exit; failed ADD CHECK copy ALTER rollback is covered for incompatible existing rows, failed ADD UNIQUE and ADD PRIMARY KEY copy ALTER rollback are covered for incompatible duplicate existing rows, representative failed OR REPLACE replacement paths preserve old targets, and representative failed multi-table `DROP` / `RENAME` paths preserve original tables, while durable transactional DDL and full handler-level transaction/savepoint semantics remain planned |
| Savepoints | 🟡&nbsp;Partial | Direct and prepared `SAVEPOINT`, `ROLLBACK TO [SAVEPOINT]`, and `RELEASE SAVEPOINT` support case-insensitive simple unquoted, backtick-quoted, and ANSI_QUOTES double-quoted savepoint names inside active bounded file-backed row-DML transactions over durable MyLite storage; duplicate savepoint replacement, rollback-to target reuse, release preserving changes, missing-savepoint diagnostics, full rollback, prepared-statement reuse, and close-time rollback are covered. Handler-level savepoint hooks, MEMORY/HEAP row savepoints, durable transactional DDL, and full MariaDB transactional engine semantics remain planned |
| Crash recovery | 🟡&nbsp;Partial | Recover the primary file from `<database>.mylite-journal` for current publication paths and from `<database>.mylite-transaction-journal` for active row-DML transactions while holding the exclusive primary-file lock; ordinary publication recovery runs before transaction-journal recovery, while WAL/checkpoints, isolation, and fully transactional engine flags remain planned |
| Multiple readers | 🟡&nbsp;Partial | Storage read APIs acquire shared advisory locks over stable committed state; configured busy timeouts wait for cooperating primary-file lock conflicts before returning busy, while SQL transaction lock integration remains planned |
| SQL locking statements | 🟡&nbsp;Partial | `LOCK TABLES`, `UNLOCK TABLES`, locking reads such as `SELECT ... FOR UPDATE` and `LOCK IN SHARE MODE`, and representative named-lock functions are rejected before MariaDB execution until table, row, named-lock, and transaction-aware lock semantics exist |
| Concurrent writers | ⚪&nbsp;Planned | Preserve the full write-concurrency goal in storage and lock design |
| Cross-process unsafe writers | 🟡&nbsp;Partial | Cooperating MyLite storage operations reject conflicting cross-process opens, writes, and recovery with busy errors after the configured timeout expires; non-MyLite writers remain out of scope |

## Application Schemas

| Capability | MyLite status | Compatibility target |
| --- | --- | --- |
| WordPress-shaped core tables | 🟡&nbsp;Partial | Storage-engine smoke covers a broader `wp_options`, `wp_posts`, `wp_postmeta`, `wp_users`, `wp_usermeta`, `wp_terms`, `wp_term_taxonomy`, `wp_term_relationships`, `wp_comments`, `wp_commentmeta`, and `wp_links` subset with `ENGINE=InnoDB` routing, `utf8mb4_unicode_ci` defaults, autoincrement ids, text payloads, composite keys, secondary and prefix indexes, joins, updates, deletes, close/reopen, and sidecar gates; it also imports WordPress 6.9.4 single-site installer DDL, installer seed fixtures with the full deterministic single-site `populate_options()` option-name inventory, selected default values and autoload flags, the full default role payload, and multisite global plus blog-id-2 schema and network seed fixtures with representative indexed network and per-blog rows through omitted-engine routing to MyLite; full WordPress runtime install, dynamic PHP/theme/localization installer output, exhaustive collation variants, full multisite runtime, and additional per-blog suites remain planned |
| BuddyPress component tables | 🟡&nbsp;Partial | Storage-engine smoke imports BuddyPress 14.4.0 full-component table DDL through omitted-engine routing with deterministic `wp_` prefix, `utf8mb4_unicode_ci` defaults, BuddyPress meta-key prefix indexes, and representative activity, notification, friends, groups, messaging, xProfile, blog-tracking, invitation, and opt-out rows with forced indexed reads before and after close/reopen; full BuddyPress runtime activation, WordPress `dbDelta()` execution, email post/term content, the WordPress core `wp_signups` table from BuddyPress install flow, and additional plugin schemas remain planned |
| Laravel default ORM tables | 🟡&nbsp;Partial | Storage-engine smoke imports deterministic MySQL/MariaDB DDL equivalent to Laravel skeleton v13.6.0 default migrations through omitted-engine routing, with `users`, `password_reset_tokens`, `sessions`, `cache`, `cache_locks`, `jobs`, `job_batches`, and `failed_jobs` tables plus representative user, session, cache, queued-job, batch, and failed-job rows with forced indexed reads before and after close/reopen; a representative Laravel-shaped FK constraint with an explicit child index is covered, while Laravel runtime compatibility, `artisan migrate`, Illuminate schema grammar execution, broader FK/migration suites, and application-level Eloquent behavior remain planned |
| Django default ORM tables | 🟡&nbsp;Partial | Storage-engine smoke imports deterministic MySQL/MariaDB DDL equivalent to Django 6.0.5 default auth, admin, contenttypes, sessions, and migration-recorder schema through omitted-engine routing, with `django_migrations`, `django_content_type`, `auth_permission`, `auth_group`, `auth_group_permissions`, `auth_user`, `auth_user_groups`, `auth_user_user_permissions`, `django_admin_log`, and `django_session` tables plus representative migration, content type, permission, group, user, many-to-many, session, and admin-log rows with forced indexed reads before and after close/reopen; a representative Django-shaped FK constraint with an explicit child index is covered, while Django runtime compatibility, migration executor support, database backend integration, broader FK/migration suites, and application-level QuerySet behavior remain planned |
| Rails Active Record component tables | 🟡&nbsp;Partial | Storage-engine smoke imports deterministic MySQL/MariaDB DDL equivalent to Rails v8.1.3 Active Record metadata, Active Storage, and Action Text install migrations through omitted-engine routing, with `schema_migrations`, `ar_internal_metadata`, `active_storage_blobs`, `active_storage_attachments`, `active_storage_variant_records`, and `action_text_rich_texts` tables plus representative migration metadata, internal metadata, blob, attachment, variant, and rich-text rows with forced indexed reads before and after close/reopen; a representative Rails-shaped FK constraint with an explicit child index is covered, while Rails runtime compatibility, migration execution, mysql2/trilogy adapter integration, broader FK/migration suites, and application-level model behavior remain planned |
| Representative collation index paths | 🟡&nbsp;Partial | Storage-engine smoke covers `utf8mb4_general_ci`, `utf8mb4_bin`, `utf8mb4_unicode_ci`, `utf8mb4_unicode_520_ci`, `utf8mb4_unicode_nopad_ci`, `utf8mb4_uca1400_ai_ci`, `utf8mb4_uca1400_as_cs`, `utf8mb4_uca1400_czech_ai_ci`, `utf8mb4_general1400_as_ci`, `utf8mb3_general_ci`, `latin1_swedish_ci`, `latin1_bin`, `latin1_general_ci`, `latin1_german2_ci`, `latin2_czech_cs`, and `cp1250_czech_cs` table metadata, duplicate-key checks, unique and secondary indexed lookups, close/reopen, and sidecar gates; exhaustive collation semantics and locale-specific comparison matrices remain planned |
